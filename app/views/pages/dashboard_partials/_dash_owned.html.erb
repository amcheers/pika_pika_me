<div class="col-12 col-lg-6">
  <h3 class="dash-container-heading">My Pokemons</h3>
  <div class="dash-container">
    <div class="grid">
     <% @owned_pokemons.each do |pokemon| %>
      <% if pokemon.kind.name== "water" %>
        <figure class="card card--water">
      <% elsif pokemon.kind.name== "fire" %>
        <figure class="card card--fire">
      <% elsif pokemon.kind.name== "electro" %>
        <figure class="card card--electric">
      <% elsif pokemon.kind.name== "rock" %>
        <figure class="card card--normal">
      <% elsif pokemon.kind.name== "poison" %>
        <figure class="card card--dark">
      <% elsif pokemon.kind.name== "fairy" %>
        <figure class="card card--fairy">
      <% elsif pokemon.kind.name== "grass" %>
        <figure class="card card--grass">
      <% elsif pokemon.kind.name== "ground" %>
        <figure class="card card--normal">
      <% elsif pokemon.kind.name== "normal" %>
        <figure class="card card--ice">
      <% else pokemon.kind.name== "bug" %>
        <figure class="card card--psychic">
      <% end %>

    <div class="edit-btn"><%= link_to "Edit", edit_pokemon_path(pokemon), class: "btn btn-light"%></div>
    <div><%= link_to pokemon_path(pokemon), method: :delete, data: { confirm: "Are you sure?"} do %>
      <div class="trash-icon"><i class="fas fa-trash"></i></div>
    <% end %>
    </div>

     <div class="card__image-container">
      <%= cl_image_tag pokemon.photo.key, crop: :fill, class:"card__image" %>
    </div>

    <figcaption class="card__caption">
      <h1 class="card__name"><%= link_to pokemon.name, pokemon_path(pokemon) %></h1>
      <h3 class="card__type"><%= pokemon.kind.name%></h3>
    </figcaption>
  </figure>
  <% end %>
</div>
<div><%= link_to "Add New Pokemon", new_pokemon_path, class: "btn btn-primary"%></div>
</div>
</div>


<div class="col-12 col-lg-6">
  <h3 class="dash-container-heading">My Pokemon Bookings</h3>
  <div class="dash-container" id="discover-new" style= "background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://freepngimg.com/thumb/pokemon/37475-6-pikachu-transparent-image.png)">
    <div id="discover-new-content">
      <h1>You Can Find New Pokemons Anywhere</h1>
      <div><%= link_to "Discover", root_path, class: "btn btn-primary"%></div>
    </div>
  </div>
</div>
